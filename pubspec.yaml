name: flame_clusterizer
description: Clusterizing game for faster rendering and collision detecting
version: 1.0.0
homepage: https://github.com/ASGAlex/flame_clusterizer

environment:
  sdk: '>=2.18.4 <3.0.0'

dependencies:
  flame: ^1.5.0
  flame_tiled: ^1.9.0
  tiled: ^0.10.1

dev_dependencies:
  lints: ^2.0.0
  test: ^1.16.0
  flutter_lints: ^2.0.0
